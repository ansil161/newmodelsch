/* ==========================================================================
   08 - THE PRINCIPAL
   --------------------------------------------------------------------------
   The same feature the About page carries, shortened to two paragraphs with
   a link through to the whole message. See `components/principal`.
   ========================================================================== */

export { HomePrincipal } from '@/components/principal/PrincipalMessage';
